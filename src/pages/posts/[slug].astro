---
// Placeholder: la plataforma ahora sirve posts desde archivos Markdown en `src/pages/posts/*.md`
const note = true;
export const prerender = true;
export const getStaticPaths = async () => [];
---

<div class="max-w-4xl mx-auto p-6 bg-white rounded-lg shadow-lg my-8">
	<h1 class="text-2xl font-semibold">Nota</h1>
	<p class="text-gray-600 mt-2">Los posts ahora se sirven desde archivos Markdown en <code>src/pages/posts/</code>.</p>
</div>
