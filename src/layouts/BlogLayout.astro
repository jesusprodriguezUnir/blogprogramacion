---
import '../styles/global.css';

const base = (import.meta as any).env.BASE_URL || '/';

interface Props {
  title: string;
  description?: string;
}

const { title, description = "Mi rincón sobre programación y tecnología" } = Astro.props;
const pathname = Astro.url.pathname;
const currentPath = pathname.replace(base, "") || "";
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <script is:inline>
      const theme = (() => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
        }
        return 'light';
      })();
      document.documentElement.setAttribute('data-theme', theme);
    </script>
  </head>
  <body>
    <header class="site-header glass">
      <nav class="nav">
        <a class="brand mono" href={base}>
          <span class="prompt-mark">>_</span> JPRC<span class="cursor">_</span>
        </a>
        <div class="nav-links">
          <a href={base} class={currentPath === "" ? "active" : ""}>~/home</a>
          <a href={`${base}enlaces`} class={currentPath.startsWith("enlaces") ? "active" : ""}>/recursos</a>
          <a href={`${base}prompts`} class={currentPath.startsWith("prompts") ? "active" : ""}>/skills</a>
          <a href={`${base}sobre-mi`} class={currentPath.startsWith("sobre-mi") ? "active" : ""}>/about</a>
        </div>
        <button id="theme-toggle" class="theme-toggle" aria-label="Cambiar tema">
          <svg class="icon-sun" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="5"></circle><line x1="12" y1="1" x2="12" y2="3"></line><line x1="12" y1="21" x2="12" y2="23"></line><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line><line x1="1" y1="12" x2="3" y2="12"></line><line x1="21" y1="12" x2="23" y2="12"></line><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line><line x1="18.36" y1="4.22" x2="19.78" y2="5.64"></line>
          </svg>
          <svg class="icon-moon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
          </svg>
        </button>
      </nav>
    </header>

    <main class="container">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="footer-inner container">
        <div class="footer-copy mono">
          <span class="prompt-mark">>_</span> {new Date().getFullYear()} JPRC <span class="separator">::</span> <a href="https://creativecommons.org/licenses/by/4.0/" target="_blank" rel="noopener">CC BY 4.0</a>
        </div>
        <div class="footer-socials">
          <a href="https://github.com/jesusprodriguezUnir" aria-label="GitHub" target="_blank" rel="noopener">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12z"/></svg>
          </a>
        </div>
      </div>
    </footer>

    <script is:inline>
      const themeToggle = document.getElementById('theme-toggle');
      themeToggle.addEventListener('click', () => {
        const currentTheme = document.documentElement.getAttribute('data-theme');
        const nextTheme = currentTheme === 'light' ? 'dark' : 'light';
        document.documentElement.setAttribute('data-theme', nextTheme);
        localStorage.setItem('theme', nextTheme);
      });
    </script>
  </body>
</html>

<style is:global>
  .site-header {
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 100;
    margin-top: 1rem;
    border-radius: 16px;
    background: rgba(17, 17, 26, 0.8) !important; /* Forces navy background */
  }

  .nav {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
    display: flex;
    align-items: center;
  }

  .brand {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--text);
    display: flex;
    align-items: center;
    gap: 0.4rem;
    letter-spacing: -0.02em;
  }

  .prompt-mark {
    color: var(--accent);
    font-weight: 900;
  }

  .cursor {
    animation: blink 1.2s infinite;
    color: var(--accent);
    font-weight: 300;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
    margin-left: 2.5rem;
  }

  .nav-links a {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--text-muted);
    font-weight: 500;
    transition: all 0.3s var(--ease);
  }

  .nav-links a:hover, .nav-links a.active {
    color: var(--accent);
    transform: translateY(-1px);
  }

  .theme-toggle {
    margin-left: auto;
    background: var(--surface);
    border: 1px solid var(--border);
    padding: 8px;
    cursor: pointer;
    color: var(--text-muted);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s var(--ease);
  }

  .theme-toggle:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: var(--surface-hover);
    box-shadow: 0 0 15px var(--accent-soft);
  }

  .icon-sun { display: block; }
  .icon-moon { display: none; }
  [data-theme="dark"] .icon-sun { display: none; }
  [data-theme="dark"] .icon-moon { display: block; }

  .site-footer {
    border-top: 1px solid var(--border);
    padding: 4rem 0;
    margin-top: 6rem;
    background: linear-gradient(to bottom, transparent, var(--bg-soft));
  }

  .footer-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--text-dim);
    font-size: 0.85rem;
  }

  .footer-copy {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .footer-socials a {
    color: var(--text-muted);
    transition: all 0.3s var(--ease);
  }

  .footer-socials a:hover {
    color: var(--accent);
    transform: scale(1.1);
  }

  .separator {
    color: var(--border);
  }
  /* Markdown / Content Styles */
  main h1 { font-size: 2.5rem; margin: 2rem 0 1.5rem; color: var(--text); line-height: 1.2; letter-spacing: -0.02em; }
  main h2 { font-size: 1.8rem; margin: 2.5rem 0 1rem; color: var(--text); border-bottom: 1px solid var(--border); padding-bottom: 0.5rem; }
  main h3 { font-size: 1.4rem; margin: 2rem 0 1rem; color: var(--accent); }
  main p { margin-bottom: 1.5rem; line-height: 1.8; color: var(--text-muted); font-size: 1.1rem; }
  main ul, main ol { margin-bottom: 1.5rem; padding-left: 1.5rem; color: var(--text-muted); }
  main li { margin-bottom: 0.5rem; }
  
  main blockquote {
    margin: 2rem 0;
    padding: 1.5rem 2rem;
    background: var(--bg-soft);
    border-left: 4px solid var(--accent);
    font-style: italic;
    color: var(--text);
    border-radius: 0 12px 12px 0;
  }

  main code {
    font-family: var(--font-mono);
    background: var(--surface-hover);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
    color: var(--accent);
  }

  main pre {
    background: #050505 !important;
    padding: 1.5rem;
    border-radius: 12px;
    border: 1px solid var(--border);
    overflow-x: auto;
    margin: 2rem 0;
  }

  main pre code {
    background: transparent;
    padding: 0;
    color: #e2e8f0;
  }

  main strong { color: var(--text); font-weight: 600; }
  main a { color: var(--accent); text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 4px; }
  main a:hover { color: var(--text); }
</style>
