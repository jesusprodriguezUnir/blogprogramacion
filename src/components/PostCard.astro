---
import { normalizeBase, postHref } from '../utils/url';
import { getCategoryColor } from '../utils/category';

const rawBase = (import.meta as any).env.BASE_URL;
const base = normalizeBase(rawBase);

interface Props {
	title: string;
	excerpt: string;
	date: string;
	category: string;
	slug: string;
	readTime?: string;
}

const { title, excerpt, date, category, slug, readTime = "3 min" } = Astro.props;

const categoryColor = getCategoryColor(category);
---

<article class="post-card">
	<div class="post-category">
		<span class={`category-tag ${categoryColor}`}>{category}</span>
		<span class="post-date">{date}</span>
	</div>
	
	<h2 class="post-title">
		<a href={postHref(rawBase, slug)} class="post-link">{title}</a>
	</h2>
	
	<p class="post-excerpt">{excerpt}</p>
	
	<div class="post-meta">
		<span class="read-time">📖 {readTime} de lectura</span>
	<a href={postHref(rawBase, slug)} class="read-more">Leer más →</a>
	</div>
</article>

<style>
	.post-card {
		background: var(--surface);
		border-radius: 20px;
		padding: 24px;
		transition: all 0.3s var(--ease);
		border: 1px solid var(--border);
		position: relative;
		overflow: hidden;
	}

	.post-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, var(--accent), transparent);
		opacity: 0.4;
		transition: opacity 0.3s var(--ease);
	}

	.post-card:hover {
		transform: translateY(-2px);
		box-shadow: 0 18px 45px -30px color-mix(in srgb, var(--text) 55%, transparent);
		border-color: var(--accent);
	}

	.post-card:hover::before {
		opacity: 1;
	}

	.post-category {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 16px;
	}

	.category-tag {
		display: inline-block;
		padding: 3px 10px;
		border-radius: 999px;
		font-size: 10px;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		font-family: var(--font-mono);
		color: var(--bg);
	}

	.bg-green-500 { background-color: var(--success); }
	.bg-blue-500 { background-color: var(--accent); }
	.bg-purple-500 { background-color: var(--secondary); }
	.bg-orange-500 { background-color: var(--amber); }
	.bg-gray-500 { background-color: var(--text-muted); }

	.post-date {
		font-size: 11px;
		color: var(--text-dim);
		font-family: var(--font-mono);
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	.post-title {
		font-size: 1.35rem;
		font-weight: 600;
		line-height: 1.3;
		margin-bottom: 12px;
		letter-spacing: -0.01em;
		font-family: var(--font-ui);
	}

	.post-link {
		color: var(--text);
		text-decoration: none;
		transition: color 0.2s var(--ease);
	}

	.post-card:hover .post-link {
		color: var(--accent);
	}

	.post-excerpt {
		color: var(--text-muted);
		font-size: 0.98rem;
		line-height: 1.7;
		margin-bottom: 16px;
	}

	.post-meta {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding-top: 16px;
		border-top: 1px solid var(--border);
	}

	.read-time {
		font-size: 11px;
		color: var(--text-dim);
		display: flex;
		align-items: center;
		gap: 6px;
		font-family: var(--font-mono);
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}

	.read-more {
		color: var(--accent);
		font-weight: 500;
		text-decoration: none;
		font-size: 11px;
		font-family: var(--font-mono);
		transition: all 0.2s var(--ease);
		letter-spacing: 0.16em;
		text-transform: uppercase;
		padding: 6px 10px;
		border-radius: 999px;
		background: var(--accent-soft);
		border: 1px solid var(--border);
	}

	.read-more:hover {
		background: var(--accent);
		color: var(--bg);
		transform: translateY(-1px);
		border-color: var(--accent);
	}

	@media (max-width: 768px) {
		.post-card {
			padding: 24px;
		}

		.post-title {
			font-size: 20px;
		}

		.post-meta {
			flex-direction: column;
			align-items: flex-start;
			gap: 12px;
		}
	}
</style>